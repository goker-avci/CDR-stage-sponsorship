kind = "web"
previewPath = "/"
title = "CDR Stage — Towards COP31"
version = "1.0.0"
id = "artifacts/cdr-stage"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 23178

[services.development]
run = "pnpm --filter @workspace/cdr-stage run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/cdr-stage", "run", "build" ]
publicDir = "artifacts/cdr-stage/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "23178"
BASE_PATH = "/"
